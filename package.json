{
  "name": "@sunday-sky/cc-switcher",
  "version": "1.0.1",
  "description": "CLI for switching Claude Code providers and models",
  "license": "MIT",
  "homepage": "https://github.com/rocsun/cc-switcher",
  "packageManager": "bun@1.2.16",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rocsun/cc-switcher.git"
  },
  "bugs": {
    "url": "https://github.com/rocsun/cc-switcher/issues"
  },
  "keywords": [
    "claude-code",
    "claude",
    "cli",
    "openrouter",
    "model-switcher",
    "provider-switcher"
  ],
  "bin": {
    "cc-switcher": "./dist/bin/cc-switcher.js",
    "ccs": "./dist/bin/cc-switcher.js"
  },
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bunx tsc --noEmit",
    "build": "rm -rf dist && bun build ./bin/cc-switcher.ts --outfile ./dist/bin/cc-switcher.js --target=node --format=esm && chmod +x ./dist/bin/cc-switcher.js",
    "prepublishOnly": "bun run typecheck && bun test && bun run build"
  },
  "engines": {
    "bun": ">=1.2.16"
  },
  "dependencies": {
    "commander": "^9.5.0",
    "enquirer": "^2.4.1",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "bun-types": "^1.2.16",
    "typescript": "^5.8.3"
  }
}
