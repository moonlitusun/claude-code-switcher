# Changelog

## 1.2.1 - 2026-04-19

### Changed

- `ccs profiles <name>` now shows only the selected profile's details instead of printing the full profile list first.
- `ccs profiles <name> --json` now returns the selected profile details directly.

### Fixed

- `ccs profiles` without a profile argument still lists saved profiles and marks the active one.

